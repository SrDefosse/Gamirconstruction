"use client";
import { useEffect, useState } from "react";
import { Link, NavLink, useLocation } from "react-router";
import { Button } from "../ui/Button";
const links = [{ to: "/process", label: "Process" }, { to: "/work", label: "Work" }, { to: "/about", label: "About" }, { to: "/services", label: "Services" }, { to: "/journal", label: "Journal" }];
export function Navbar() { const { pathname } = useLocation(); const [hasPassedHero, setHasPassedHero] = useState(false); const [isMenuOpen, setIsMenuOpen] = useState(false); useEffect(() => { const onScroll = () => setHasPassedHero(window.scrollY > window.innerHeight * .58); onScroll(); window.addEventListener("scroll", onScroll, { passive: true }); return () => window.removeEventListener("scroll", onScroll); }, [pathname]); useEffect(() => { setIsMenuOpen(false); }, [pathname]); const isDark = pathname !== "/" || hasPassedHero; return <header className={`site-header ${isDark ? "site-header--dark" : "site-header--light"}`}><nav className="nav shell" aria-label="Main navigation"><Link className="brand-logo" to="/" aria-label="Gamir Construction home"><img src="/logo.avif" alt="Gamir Construction" /></Link><div className="nav-links">{links.map((link) => <NavLink key={link.to} to={link.to}>{link.label}</NavLink>)}</div><div className="nav-actions"><Button to="/contact" variant={isDark ? "dark" : "light"}>Start a conversation</Button><button className={`nav-toggle ${isMenuOpen ? "is-open" : ""}`} type="button" aria-label={isMenuOpen ? "Close navigation" : "Open navigation"} aria-expanded={isMenuOpen} aria-controls="mobile-navigation" onClick={() => setIsMenuOpen((open) => !open)}><span /><span /></button></div></nav><button className={`nav-scrim ${isMenuOpen ? "is-open" : ""}`} type="button" tabIndex={isMenuOpen ? 0 : -1} aria-label="Close navigation" onClick={() => setIsMenuOpen(false)} /><aside id="mobile-navigation" className={`mobile-nav ${isMenuOpen ? "is-open" : ""}`} aria-hidden={!isMenuOpen}><div className="mobile-nav__top"><img src="/logo.avif" alt="Gamir Construction" /><button type="button" aria-label="Close navigation" onClick={() => setIsMenuOpen(false)}>Close <span>×</span></button></div><div className="mobile-nav__links">{links.map((link, index) => <NavLink key={link.to} to={link.to} style={{ transitionDelay: `${isMenuOpen ? 90 + index * 55 : 0}ms` }} onClick={() => setIsMenuOpen(false)}>{link.label}<span>↗</span></NavLink>)}</div><div className="mobile-nav__contact"><p>Start a project</p><a href="tel:+12107997222">(210) 799-7222</a><a href="mailto:info@gamirconstruction.com">info@gamirconstruction.com</a></div></aside></header>; }
