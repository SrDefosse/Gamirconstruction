"use client";
import { useRef } from "react";
import { useGSAP } from "@gsap/react";
import { gsap, ScrollTrigger } from "../../shared/lib/gsap";
import { images } from "../../shared/lib/images";
const steps = [["Find the right ground", "We evaluate how a site can support the life you want to build."], ["Shape the vision", "Plans, materials, and budget come into focus together."], ["Build with clarity", "A coordinated team moves each milestone forward with care."], ["Move in with confidence", "We finish thoughtfully and stay present through the handoff."]];
export function ProcessStory() { const ref = useRef<HTMLElement>(null); useGSAP(() => { if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return; const cards = gsap.utils.toArray<HTMLElement>(".step-card"); gsap.from(cards, { y: 72, opacity: 0, stagger: 0.15, scrollTrigger: { trigger: ref.current, start: "top 70%" } }); ScrollTrigger.create({ trigger: ref.current, start: "top top+=112", end: "bottom bottom+=112", pin: ".process-title", pinSpacing: false }); }, { scope: ref }); return <section ref={ref} className="process-story section shell"><div className="process-title"><p className="kicker">A collaborative journey</p><h2>Guidance you can feel at every stage.</h2></div><div className="steps">{steps.map(([title, body], i) => <article className="step-card" key={title}><span>0{i + 1}</span><img src={[images.landscape, images.detail, images.exterior, images.interior][i]} alt="Gamir project process" /><h3>{title}</h3><p>{body}</p></article>)}</div></section>; }
