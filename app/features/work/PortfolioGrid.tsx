"use client";
import { useState } from "react";
import { portfolioProjects, type PortfolioProject } from "../../shared/lib/portfolio";
const filters: Array<"All work" | PortfolioProject["category"]> = ["All work", "Custom Home", "Remodel", "In Progress"];
export function PortfolioGrid() { const [filter, setFilter] = useState<(typeof filters)[number]>("All work"); const visibleProjects = filter === "All work" ? portfolioProjects : portfolioProjects.filter((project) => project.category === filter); return <><div className="filter-row"><label className="work-filter"><span className="sr-only">Filter projects</span><svg aria-hidden="true" viewBox="0 0 24 24"><path d="M4 5h16l-6.4 7.25v5.1l-3.2 1.8v-6.9L4 5Z" /></svg><select value={filter} onChange={(event) => setFilter(event.target.value as (typeof filters)[number])}>{filters.map((item) => <option key={item} value={item}>{item}</option>)}</select></label></div><div className="work-grid">{visibleProjects.map((project) => <article className="work-card" key={project.name}><img src={project.image} alt={`${project.name} by Gamir Construction`} /><div><p>{project.category}</p><h2>{project.name}</h2></div></article>)}</div></>; }
